// Template.modeling2.helpers({
  // 'selectedWorkItem': function(){ return Session.get('selectedWorkItem'); }

// });

// Template.modeling2.events({
  // 'click .select_item': function(){
    // Session.set('selectedWorkItem', this);
    // return false;

  // }

// });
